echo "# CI-CD" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Aman-kumar-nitm/CI-CD.git
git push -u origin main

changing branch and pushing code 
git branch
git checkout -b "branch-name"

commit one change and reset it to working directory

now we will understand merging 
first go to amanDev
then write something
push to the amanDev branch 
go to main merge that amanDev to main

now we will handle merge conflict in this line only we will write something in main and then commit here and there also and then we will merge.

Here only things are written from the amanDev